build:
  context: ${{ resolve ./build }}
  dockerfile: ${{ resolve ./build/Dockerfile }}
volumes:
  - ${HOME}/.ssh/known_hosts:/home/${{ user }}/.ssh/known_hosts
  - ${{ mount_ssh_auth_sock }}
  - ${{ mount_workspace }}
