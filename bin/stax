#!/bin/bash
set -e

if [ "$(readlink $0)" != "" ]; then
  # is a symlink
  stax_home=$(readlink $0)
else
  stax_home="$0"
fi

stax_home=$(dirname "$stax_home")
stax_home=$(realpath "$stax_home/..")
WORKING_DIRECTORY=$(pwd) bun --cwd $stax_home src/cli.ts $@
